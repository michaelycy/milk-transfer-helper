# 模块 H · 账户与基础

| 元素 | 内容 |
|---|---|
| 文档状态 | 草案（FR-H0 已评审，例外先行） |
| 所属闭环 | 全部闭环的地基 |
| 上游依赖 | 无 |
| 下游被依赖 | 所有模块 |
| 覆盖需求 | FR-H0 ~ FR-H5 |
| 数据表 | `users`（openid 回填）、各业务表 RLS |

## 1. 模块定位

账户层解决"数据跟着人走"：匿名登录（现状）换手机数据即丢，与宝妈换机/多设备的真实场景冲突。方向：匿名可用的前提下提供微信登录升级，并在升级时无损迁移数据。FR-H0 是全局阻塞项——不修复页面层与认证层的断裂，任何需求都无法在真机验证。

## 2. 需求明细

### FR-H0 技术债修复（前置条件，阻塞一切） `P0`｜依赖：无｜闭环：全部｜**状态：已评审（例外先行）**
- 描述：三个页面（index/records/profile）与 service 层对齐：
  - 移除 `getUserId()` 本地 UUID 路径（`src/utils/user.ts`）与已不存在的 `IRecord` 类型引用，统一走 `ensureSession()` 匿名会话（`src/utils/auth.ts`）；
  - `getRecords` 调用方式与 `{ data, total }` 返回结构对齐（当前页面按数组使用）；
  - `addRecord` 不再传 `user_id`（由数据库 `auth.uid()` 默认填充，传错会被 RLS 拒绝）；
  - 清理 `src/pages/index/index.tsx:26-31` 遗留调试注释。
- 验收标准：
  - [ ] TypeScript 编译零错误；
  - [ ] 真机走通"匿名登录 → 记录 → 列表 → 图表"全链路；
  - [ ] RLS 插入不再被拒。

### FR-H1 微信登录与数据迁移 `P0`｜依赖：H0｜闭环：全部
- 描述：微信授权登录替换纯匿名会话（`users.openid` 字段已预留）；老匿名用户升级登录时，其记录/收藏等数据**无损迁移**到正式账号。可行性说明：Supabase 无原生微信登录，采用 Edge Function 以 code 换 openid 并签发会话，技术方案在技术文档中细化。
- 验收标准：
  - [ ] 同一微信号重复登录为同一账号；
  - [ ] 匿名→微信升级后旧数据完整可访问；
  - [ ] 迁移幂等可重试，失败不产生重复数据；
  - [ ] 拒绝授权用户仍可匿名使用（明确提示数据不跨设备）。

### FR-H2 隐私中心（导出与注销） `P1`｜依赖：H1｜闭环：合规（NFR-2）
- 描述：设置内提供：隐私政策入口、订阅消息管理、**个人信息导出**（喂养/打卡/计划记录，对应 PIPL 查阅复制权）、**账号注销（级联删除全部数据）**。
- 验收标准：
  - [ ] 注销二次确认并明示后果；
  - [ ] 删除后服务端无残留业务数据（auth 级联已具备）；
  - [ ] 导出仅含本人数据，生成可下载文件（文本/图片），失败可重试；
  - [ ] 建档前单独同意留痕（与 FR-A1 联动）。

### FR-H3 家庭共享 `P3`｜依赖：H1、A2｜闭环：L1
- 描述：爸爸/奶奶以只读或可写身份加入宝宝档案，共同记录。涉及 RLS 重设计（成员关系表），仅记录意图。
- 重新评估条件：M2 上线后用户调研中多人记录诉求占比 ≥ 30%。

### FR-H4 埋点与分析基建 `P0`（M1）｜依赖：H0｜闭环：全部（指标地基）
- 描述：统一事件模型与采集，是 glossary §5 全部指标的唯一数据源：事件表 `analytics_events`（03 §1），事件清单与指标公式见 [00-glossary.md](../00-glossary.md) §5（含 `app_launch(scene, first)`、`plan_created`、`feed_recorded(source, dur_ms)`、`alert_shown/alert_acked`、`share_card_created` 等 15 个事件）。初期以 Supabase 视图/仪表盘出数，不自建 BI。上报为 fire-and-forget：失败静默降级、不自动重试、不阻塞任何业务操作（可接受少量丢失）。
- 验收标准：
  - [ ] glossary §5 全部指标可由 `analytics_events` 表计算；
  - [ ] 分享（`scene=share_plan`）与问卷入口的归因参数落地到 `app_launch`；
  - [ ] 上报失败不影响主流程（弱网下无感）；
  - [ ] 事件仅可插入本人数据（无客户端 select 权限），看板走服务端角色（03 §2）。

### FR-H5 开屏页与全局未登录态 `P0`｜依赖：H1｜闭环：全部
- 描述：小程序启动先进入开屏页（品牌展示，停留 ≤1.5s 自动路由：已登录直达首页，未登录进入登录页）；四个 Tab 页在未登录态展示统一的「登录门」引导，不触发任何数据请求。登录页提供微信一键登录（走 FR-H1 Edge Function）与游客模式（匿名会话）兜底，《隐私政策》勾选为登录前置条件。
- 验收标准：
  - [ ] 已登录用户经开屏页无感直达首页（会话静默恢复，失败降级登录页）；
  - [ ] 未登录访问任何 Tab 页均展示登录引导，且不产生任何 Supabase 请求；
  - [ ] 隐私勾选为登录/游客入口的前置条件（留痕口径与 FR-A1/H2 一致）；
  - [ ] 游客模式入口明确提示「数据仅保存在本设备」（FR-H1 验收联动）；
  - [ ] 微信登录失败（Edge Function 未配置）时提示降级路径，不阻塞游客使用；
  - [ ] 埋点：`app_launch(scene)`、`login_success(mode)` 可统计（FR-H4）。

## 3. UI 画板对照（docs/ui/ui.pen）

> 更新（2026-09-09）：V2 系列画板已生成至 docs/ui/ui.pen（画板名以「V2-」开头）。
> 更新（2026-09-10）：画布按「一个页面一行」重排（同一页面的全部状态同排相邻，如首页行 = 01 / V2-01 / V2-02 / V2-16）；新增游客态画板 V2-16、V2-17。

| 画板 | 内容 | 覆盖 FR | 状态 |
|---|---|---|---|
| V2-12 微信登录（画板 09） | 微信一键登录 + 匿名兜底 + 隐私勾选 | H1 | 已生成 |
| V2-13 宝宝建档 | 首次使用引导（含隐私单独同意） | H2/A1 | 已生成 |
| V2-14 开屏页 | 品牌展示 + 登录态路由 | H5 | 已生成 |
| V2-15 Tab 未登录态 | 四 Tab 统一「登录门」引导 | H5 | 已生成 |
| V2-16 首页·游客模式（未建档） | 游客首进首页：顶部建档引导卡（无宝宝栏）+ **保留「推荐文章」模块**（游客可浏览）；已建档游客与登录用户一致（V2-01/V2-02） | H1/H5/G1 | 已生成 |
| V2-17 知识·游客模式 | 游客态知识页：可完整浏览文章列表与详情，数据仅本设备；未登录展示 V2-15 登录门 | H1/G1 | 已生成 |
| 选奶问卷（G3） | M3 设计阶段补齐 | G3 | 未生成 |
