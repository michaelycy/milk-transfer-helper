# Supabase 项目配置（与 apps/miniapp/.env 同值；anon key 是公开客户端凭据，按环境注入而非硬编码。
# 管理端写权限由数据库端 admins 白名单 + RLS 控制，service_role 永不下发到浏览器）
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
